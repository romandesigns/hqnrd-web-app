/**
 * Path to the directory containing locale flag assets.
 *
 * @remarks
 * This constant defines the relative path to the folder where flag images
 * for different locales are stored.
 *
 * @example
 * ```typescript
 * const flagPath = `${flags}/en-US.png`;
 * ```
 */

export const flags = "assets/flags/locales";
